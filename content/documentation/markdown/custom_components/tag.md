---
layout: layouts/documentation.njk
title: BlastoDB — Tag Component
description: Inline metadata pill for displaying subtypes, categories, and labels.
---

[hero: text-align:center]

<h-hero>Tag</h-hero>

A small inline pill for displaying structured metadata like subtypes, data types, or categories.

[:hero]

[btn: ← Custom Components -> /documentation/markdown/custom_components/]

## What it does

The tag component renders a compact, styled badge inline with your text. It is ideal for marking subtypes, data types, detection methods, and other short labels that need to stand out visually.

## Syntax

```markdown
[tag: Label]
```

The label can be any short text. Tags are **inline elements** — they sit within a sentence or a list item, not as standalone blocks.

## Example

```markdown
The dataset contains [tag: Genomics] and [tag: Proteomics] data from Germany.
```

The dataset contains [tag: Genomics] and [tag: Proteomics] data from Germany.

---
[box: text-align:center]
[btn: ← Back to Custom Components -> /documentation/markdown/custom_components/]
[:box]
