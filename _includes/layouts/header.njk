<header>
  <div class="header-inner">
    <a href="{{ '/' | url }}" class="site-logo">
      <img src="{{ collections.branding.site_logo | url }}" alt="Blastie mascot" class="logo-image">
      <span>{{ collections.branding.site_title }}</span>
    </a>
    <header>
      {% set currentFileName = page.filePathStem | replace('/index', '') %}
      {% set navItems = collections.header.items %}
      {% for item in navItems %}
        {% if item.type == 'link' %}
          {% set itemPath = '/' + item.file.replace('content/', '').replace('.md', '') %}
          <a href="{{ item.url | url }}" {% if itemPath == currentFileName %}class="active"{% endif %}>
            {{ item.label }}
          </a>
        {% elif item.type == 'dropdown' %}
          {% set uniqueId = 'header-dropdown-' + loop.index %}
          <div class="header-dropdown-container">
            <button class="header-dropdown-btn" data-target="{{ uniqueId }}" aria-expanded="false">
              {{ item.label }}
              <span class="header-dropdown-arrow">▼</span>
            </button>
            <div class="header-dropdown-menu" id="{{ uniqueId }}">
              {% for subItem in item.items %}
                {% set subItemPath = '/' + subItem.file.replace('content/', '').replace('.md', '') %}
                <a href="{{ subItem.url | url }}" class="header-dropdown-item" {% if subItemPath == currentFileName %}data-active="true"{% endif %}>
                  {{ subItem.label }}
                </a>
              {% endfor %}
            </div>
          </div>
        {% endif %}
      {% endfor %}
    </header>
    <button class="header-toggle" aria-label="Toggle navigation">☰</button>
  </div>
</header>
